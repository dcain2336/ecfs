"""ECFS Lite — multi-tenant HTTP gateway for light users and agents.

Lite users can:
- Send messages to their own nodes via encrypted mesh
- Access their own sensor data (not others')
- Receive files/messages via their nodes
- Use shared compute from ECFS nodes (if enabled)
- Install lite nodes on personal devices

Lite nodes act as gateways and cannot see other lite users' data.
"""

__version__ = "0.1.0"
